# Пошаговая инструкция: Как настроить уведомления

## ✅ Шаг 1: Проверить, задеплоен ли сервер

### Вариант A: Сервер уже задеплоен

1. Откройте [Render.com Dashboard](https://dashboard.render.com)
2. Найдите ваш Web Service (скорее всего называется `habit-tracker-notifications`)
3. Скопируйте URL (например: `https://habit-tracker-notifications.onrender.com`)
4. Проверьте, что сервер работает:
   - Откройте в браузере: `https://ваш-сервер.onrender.com/health`
   - Должен вернуться JSON: `{"status":"ok",...}`

### Вариант B: Сервер НЕ задеплоен

**Нужно задеплоить сервер на Render.com:**

1. Зарегистрируйтесь на [render.com](https://render.com) (можно через GitHub)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите репозиторий `Tilkerman/project-pwa`
4. Настройки:
   - **Name**: `habit-tracker-notifications`
   - **Region**: `Frankfurt` (или ближайший к вам)
   - **Branch**: `main`
   - **Root Directory**: `server` ⚠️ **ВАЖНО!**
   - **Runtime**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Environment Variables**:
     - `TELEGRAM_BOT_TOKEN` = `7985172630:AAFTN9xGM2C-s0ni07VHOcCCZiyL3XbDh4o`
5. Нажмите **"Create Web Service"**
6. Подождите 2-3 минуты, пока сервер задеплоится
7. Скопируйте URL сервера (будет показан в Dashboard)

---

## ✅ Шаг 2: Обновить URL сервера в коде

После того, как получили URL сервера (например: `https://habit-tracker-notifications.onrender.com`):

### Вариант 1: Через переменную окружения (Рекомендуется)

1. Создайте файл `.env` в корне проекта (рядом с `package.json`)
2. Добавьте строку:
   ```
   VITE_NOTIFICATION_SERVER_URL=https://ваш-сервер.onrender.com
   ```
3. Пересоберите приложение:
   ```bash
   npm run build
   ```
4. Закоммитьте и запушьте изменения

### Вариант 2: Прямо в коде

1. Откройте `src/utils/notificationServer.ts`
2. Найдите строку 12:
   ```typescript
   const defaultRenderUrl = 'https://habit-tracker-notifications.onrender.com'
   ```
3. Замените на ваш реальный URL:
   ```typescript
   const defaultRenderUrl = 'https://ваш-реальный-сервер.onrender.com'
   ```
4. Закоммитьте и запушьте изменения

---

## ✅ Шаг 3: Настроить пробуждение сервера (КРИТИЧНО!)

**Проблема:** На бесплатном тарифе Render.com сервер засыпает после 15 минут бездействия.

**Решение:** Использовать внешний cron для пробуждения каждые 10 минут.

### Способ 1: cron-job.org (Самый простой)

1. Зарегистрируйтесь на [cron-job.org](https://cron-job.org) (бесплатно)
2. Нажмите **"Create cronjob"**
3. Заполните:
   - **Title**: `Wake Render Server`
   - **Address (URL)**: `https://ваш-сервер.onrender.com/wake`
   - **Schedule**: Выберите **"Every 10 minutes"** или введите `*/10 * * * *`
   - **Request Method**: `GET`
4. Нажмите **"Create cronjob"**
5. Готово! Теперь сервер будет пробуждаться каждые 10 минут

### Способ 2: GitHub Actions (Если репозиторий на GitHub)

Я могу создать автоматический workflow для пробуждения сервера через GitHub Actions.

---

## ✅ Шаг 4: Проверить работу

### 4.1. Проверить сервер

Откройте в браузере:
- `https://ваш-сервер.onrender.com/health` - должен вернуть JSON
- `https://ваш-сервер.onrender.com/wake` - должен вернуть `{"status":"awake",...}`

### 4.2. Проверить логи сервера

1. Откройте Render.com Dashboard → ваш сервис → вкладка **"Logs"**
2. Должны видеть:
   - `🚀 Сервер запущен на порту 3000`
   - `🕐 Проверка уведомлений в XX:XX` (каждую минуту)
   - `📊 Активных расписаний: X из Y` (каждые 5 минут)
   - `🔔 Сервер пробужден` (если настроен внешний cron)

### 4.3. Протестировать в приложении

1. Откройте приложение в Telegram Mini App
2. Создайте привычку с уведомлениями
3. Установите время уведомления (например, через 2 минуты)
4. Откройте консоль браузера (F12) и проверьте логи:
   - Должно быть: `📤 Отправка расписания на сервер: ...`
   - Должно быть: `✅ Расписание отправлено на сервер уведомлений`
5. Закройте приложение полностью
6. Подождите указанное время
7. Уведомление должно прийти от бота @habitnotibot

---

## 🔍 Диагностика проблем

### Проблема: "Сервер уведомлений не настроен"

**Решение:** Проверьте, что URL сервера правильный в `src/utils/notificationServer.ts` или в `.env`

### Проблема: "Не удалось отправить расписание на сервер"

**Решение:**
1. Проверьте, что сервер задеплоен и работает (`/health` endpoint)
2. Проверьте логи сервера в Render.com Dashboard
3. Убедитесь, что сервер не спит (настроен внешний cron)

### Проблема: Уведомления не приходят

**Решение:**
1. Проверьте логи сервера - должны быть сообщения `🕐 Проверка уведомлений`
2. Проверьте, что расписание сохранено: `https://ваш-сервер.onrender.com/api/schedules`
3. Убедитесь, что `chatId` правильный (откройте приложение в Telegram Mini App)
4. Проверьте, что сервер не спит (настроен внешний cron каждые 10 минут)

### Проблема: Сервер засыпает

**Решение:** Обязательно настройте внешний cron (cron-job.org) для пробуждения каждые 10 минут

---

## 📝 Чек-лист

- [ ] Сервер задеплоен на Render.com
- [ ] URL сервера обновлен в коде или `.env`
- [ ] Приложение пересобрано и задеплоено
- [ ] Настроен внешний cron для пробуждения (cron-job.org)
- [ ] Сервер отвечает на `/health` и `/wake`
- [ ] В логах сервера видны сообщения о проверке уведомлений
- [ ] Создана тестовая привычка с уведомлениями
- [ ] Тестовое уведомление пришло в Telegram

---

## 🎯 Итого

**Минимально необходимые шаги:**
1. ✅ Задеплоить сервер на Render.com (если еще не задеплоен)
2. ✅ Обновить URL сервера в коде
3. ✅ Настроить внешний cron для пробуждения (cron-job.org)
4. ✅ Протестировать

После этого уведомления будут работать даже когда приложение закрыто!


