# Настройка Telegram Mini App - Пошаговая инструкция

## ✅ Что уже сделано

1. ✅ Установлен Telegram Web App SDK (`@twa-dev/sdk`)
2. ✅ Создана утилита для работы с Telegram API (`src/utils/telegramMiniApp.ts`)
3. ✅ Добавлена инициализация SDK в `main.ts`
4. ✅ Создан composable для компонентов (`src/composables/useTelegram.ts`)
5. ✅ Адаптирована тема под Telegram

## 🚀 Последний шаг: Настройка бота

Теперь нужно настроить ваш Telegram бот, чтобы добавить кнопку меню для открытия Mini App.

### Вариант 1: Через BotFather (простой способ)

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/mybots`
3. Выберите вашего бота из списка
4. Выберите **"Bot Settings"** → **"Menu Button"**
5. Выберите **"Configure Menu Button"**
6. Введите текст кнопки: `Открыть приложение`
7. Выберите **"Web App"**
8. Введите URL вашего приложения: `https://tilkerman.github.io/project-pwa/`
9. Готово! ✅

Теперь в вашем боте появится кнопка меню внизу, которая открывает Mini App.

### Вариант 2: Через код (программно)

Вы можете использовать утилиту `setupTelegramBotMenu` из `src/utils/setupTelegramBot.ts`:

```typescript
import { setupTelegramBotMenu } from '@/utils/setupTelegramBot'

// Установить кнопку меню
const result = await setupTelegramBotMenu('https://tilkerman.github.io/project-pwa/')
if (result.success) {
  console.log('✅ Кнопка меню установлена!')
} else {
  console.error('❌ Ошибка:', result.error)
}
```

### Вариант 3: Через curl (для тестирования)

```bash
curl -X POST "https://api.telegram.org/bot<ВАШ_ТОКЕН>/setChatMenuButton" \
  -H "Content-Type: application/json" \
  -d '{
    "menu_button": {
      "type": "web_app",
      "text": "Открыть приложение",
      "web_app": {
        "url": "https://tilkerman.github.io/project-pwa/"
      }
    }
  }'
```

Замените `<ВАШ_ТОКЕН>` на токен вашего бота из `src/config/telegram.ts`.

## 🧪 Как протестировать

1. **Откройте вашего бота в Telegram**
2. **Нажмите на кнопку меню** (обычно внизу экрана, рядом с полем ввода)
3. **Должна появиться кнопка "Открыть приложение"**
4. **Нажмите на неё** - ваше PWA должно открыться внутри Telegram!

## 📱 Что будет работать в Telegram Mini App

- ✅ Все функции приложения (создание привычек, календарь, статистика)
- ✅ Автоматическое определение темы Telegram (светлая/тёмная)
- ✅ Данные пользователя Telegram (имя, фото, ID)
- ✅ Вибрация при действиях (опционально)
- ✅ Закрытие приложения программно
- ✅ Кнопка "Назад" в Telegram

## 🔧 Дополнительные возможности

### Использование в компонентах

```vue
<script setup lang="ts">
import { useTelegram } from '@/composables/useTelegram'

const { 
  isInTelegram,      // true, если запущено в Telegram
  userName,          // Имя пользователя Telegram
  userPhoto,         // Фото пользователя
  vibrate,           // Вибрация
  closeApp,          // Закрыть приложение
  setMainButton,     // Настроить главную кнопку
} = useTelegram()

// Пример использования
function handleSave() {
  vibrate('medium')  // Вибрация при сохранении
  notifySuccess()    // Уведомление об успехе
}
</script>
```

### Настройка главной кнопки Telegram

```typescript
import { useTelegram } from '@/composables/useTelegram'

const { setMainButton } = useTelegram()

// Показать кнопку "Сохранить" внизу экрана Telegram
setMainButton('Сохранить', () => {
  // Действие при нажатии
  saveHabit()
}, true)
```

## ⚠️ Важные моменты

1. **HTTPS обязателен** - Mini Apps работают только по HTTPS (у вас уже есть)
2. **Домен должен быть доступен** - убедитесь, что приложение доступно по указанному URL
3. **Тестирование** - лучше тестировать в реальном Telegram, а не в браузере
4. **Кэш** - если изменения не видны, очистите кэш Telegram или перезапустите приложение

## 🐛 Решение проблем

### Кнопка меню не появляется
- Убедитесь, что токен бота правильный
- Проверьте, что URL доступен по HTTPS
- Попробуйте перезапустить Telegram

### Приложение не открывается
- Проверьте, что URL правильный и доступен
- Убедитесь, что домен не заблокирован
- Проверьте консоль браузера на ошибки

### Тема не применяется
- Убедитесь, что `initTelegramMiniApp()` вызывается в `main.ts`
- Проверьте консоль на ошибки инициализации

## 📚 Дополнительная информация

- [Telegram Mini Apps Docs](https://core.telegram.org/bots/webapps)
- [@twa-dev/sdk GitHub](https://github.com/twa-dev/sdk)
- [Telegram Bot API](https://core.telegram.org/bots/api)

## ✅ Готово!

После настройки кнопки меню ваше приложение будет полностью интегрировано с Telegram Mini Apps!


