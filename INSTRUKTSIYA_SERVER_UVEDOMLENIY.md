# Инструкция по настройке сервера уведомлений

## 🎯 Что это даёт

Сервер уведомлений позволяет отправлять уведомления через Telegram Bot API **даже когда приложение полностью закрыто**. Сервер работает 24/7 и проверяет расписание каждую минуту.

## 🚀 Быстрый деплой на Render.com (бесплатно)

### Шаг 1: Регистрация на Render

1. Перейдите на [render.com](https://render.com)
2. Зарегистрируйтесь (можно через GitHub)
3. Подтвердите email

### Шаг 2: Создание Web Service

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий `Tilkerman/project-pwa`
3. Настройки:
   - **Name**: `habit-tracker-notifications` (или любое другое имя)
   - **Region**: `Frankfurt` (ближайший к России)
   - **Branch**: `main`
   - **Root Directory**: `server` (важно!)
   - **Runtime**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Environment Variables** (опционально):
     - `TELEGRAM_BOT_TOKEN` = `7985172630:AAFTN9xGM2C-s0ni07VHOcCCZiyL3XbDh4o`
4. Нажмите **"Create Web Service"**

### Шаг 3: Получение URL сервера

После деплоя (займёт 2-3 минуты) вы получите URL вида:
```
https://habit-tracker-notifications.onrender.com
```

### Шаг 4: Настройка в приложении

1. Откройте файл `src/utils/notificationServer.ts`
2. Замените URL на ваш:
```typescript
const NOTIFICATION_SERVER_URL = 'https://ваш-сервер.onrender.com'
```

Или создайте файл `.env` в корне проекта:
```
VITE_NOTIFICATION_SERVER_URL=https://ваш-сервер.onrender.com
```

3. Пересоберите приложение:
```bash
npm run build
```

4. Закоммитьте и запушьте изменения

## ✅ Проверка работы

1. Откройте приложение в Telegram Mini App
2. Создайте привычку с уведомлениями
3. Установите время уведомления (например, через 2 минуты)
4. Закройте приложение полностью
5. Подождите указанное время
6. Уведомление должно прийти от бота @habitnotibot!

## 🔧 Альтернативные варианты деплоя

### Railway.app (тоже бесплатно)

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Railway автоматически определит Node.js проект
5. Укажите **Root Directory**: `server`
6. Railway автоматически задеплоит

### Vercel (Serverless)

Можно использовать Vercel, но для cron нужен внешний сервис типа [cron-job.org](https://cron-job.org) для периодических запросов.

## 📊 Мониторинг

После деплоя вы можете проверить логи сервера в панели Render:
- Перейдите в ваш Web Service
- Откройте вкладку **"Logs"**
- Вы увидите логи проверки уведомлений каждую минуту

## 🧪 Тестирование API

### Проверка здоровья сервера:
```bash
curl https://ваш-сервер.onrender.com/health
```

### Добавление расписания:
```bash
curl -X POST https://ваш-сервер.onrender.com/api/schedule \
  -H "Content-Type: application/json" \
  -d '{
    "habitId": "test-123",
    "chatId": "ваш-chat-id",
    "habit": {
      "name": "Тест",
      "notificationTime": "12:00",
      "notificationEnabled": true,
      "character": "babushka"
    }
  }'
```

### Получение всех расписаний:
```bash
curl https://ваш-сервер.onrender.com/api/schedules
```

## ⚠️ Важно

1. **Бесплатный тариф Render** имеет ограничения:
   - Сервер "засыпает" после 15 минут неактивности
   - Первый запрос после пробуждения может быть медленным (холодный старт)
   - Для постоянной работы нужен платный тариф ($7/месяц)

2. **Решение для бесплатного тарифа**:
   - Используйте внешний cron-сервис (например, [cron-job.org](https://cron-job.org))
   - Настройте его на запрос `/health` каждые 10 минут
   - Это будет держать сервер "живым"

3. **Альтернатива**: Используйте Railway.app - у них более щедрый бесплатный тариф

## 🎉 Готово!

После настройки сервера уведомления будут работать даже когда приложение полностью закрыто!






