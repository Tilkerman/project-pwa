# 📱 Как создать Telegram бота - пошаговая инструкция

## Что делаю Я (AI):
✅ Создал файлы с кодом  
✅ Настроил систему уведомлений  
✅ Подготовил места для токена  

## Что делаете ВЫ:

### Шаг 1: Создание бота в Telegram (5 минут)

1. **Откройте Telegram** на телефоне или компьютере

2. **Найдите @BotFather** в поиске Telegram
   - Это официальный бот от Telegram (он уже существует, его создавать не нужно!)
   - Просто найдите его в поиске

3. **Нажмите "Start"** или отправьте команду `/start`

4. **Отправьте команду** `/newbot`

5. **BotFather спросит имя бота:**
   - Напишите любое имя, например: `Трекер Привычек`
   - Нажмите Enter

6. **BotFather спросит username бота:**
   - Напишите username (должен заканчиваться на `bot`)
   - Например: `habit_tracker_bot` или `my_habits_bot`
   - Нажмите Enter

7. **BotFather отправит вам токен!**
   - Выглядит примерно так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
   - **ВАЖНО: Скопируйте этот токен!** Он понадобится дальше

### Шаг 2: Вставка токена в код

После того как вы получили токен, нужно вставить его в 2 места:

#### Место 1: `src/config/telegram.ts`

1. Откройте файл `src/config/telegram.ts`
2. Найдите строку:
   ```typescript
   export const TELEGRAM_BOT_TOKEN = 'YOUR_BOT_TOKEN_HERE'
   ```
3. Замените `YOUR_BOT_TOKEN_HERE` на ваш токен:
   ```typescript
   export const TELEGRAM_BOT_TOKEN = '123456789:ABCdefGHIjklMNOpqrsTUVwxyz'
   ```
   (вставьте ваш реальный токен вместо примера)

#### Место 2: `public/sw-custom.js`

1. Откройте файл `public/sw-custom.js`
2. Найдите строку (примерно строка 112):
   ```javascript
   const TELEGRAM_BOT_TOKEN = 'YOUR_BOT_TOKEN_HERE'
   ```
3. Замените `YOUR_BOT_TOKEN_HERE` на тот же токен:
   ```javascript
   const TELEGRAM_BOT_TOKEN = '123456789:ABCdefGHIjklMNOpqrsTUVwxyz'
   ```

### Шаг 3: Пересборка приложения

После вставки токена выполните в терминале:

```bash
npm run build
```

Или если используете dev-режим, перезапустите:

```bash
npm run dev
```

## ✅ Готово!

После этого пользователям нужно будет только:
1. Получить свой Chat ID через @userinfobot
2. Ввести его в настройках приложения

## ❓ Частые вопросы:

**Q: Что если я потерял токен?**  
A: Откройте @BotFather → отправьте `/mybots` → выберите вашего бота → нажмите "API Token"

**Q: Можно ли использовать один бот для всех пользователей?**  
A: Да! Именно так и работает. Один бот отправляет сообщения всем пользователям по их Chat ID.

**Q: Безопасно ли хранить токен в коде?**  
A: Для PWA без сервера это нормально. Токен будет виден в коде, но это не критично для простого бота уведомлений.

## 🆘 Если что-то не работает:

1. Проверьте, что токен скопирован полностью (без пробелов)
2. Убедитесь, что токен вставлен в ОБА файла
3. Пересоберите приложение после изменений
4. Проверьте токен через @BotFather командой `/mybots`





