# Настройка пробуждения Render.com для уведомлений

## Проблема

На **бесплатном тарифе Render.com** сервер "засыпает" после 15 минут бездействия. Это означает, что:
- Cron-задачи не выполняются, когда сервер спит
- Уведомления не отправляются
- Первый запрос после пробуждения может быть медленным (холодный старт)

## Решение

Используйте внешний сервис для периодического пробуждения сервера.

### Вариант 1: cron-job.org (Рекомендуется)

1. Зарегистрируйтесь на [cron-job.org](https://cron-job.org) (бесплатно)
2. Создайте новую задачу:
   - **URL**: `https://ваш-сервер.onrender.com/wake`
   - **Расписание**: Каждые 10 минут (`*/10 * * * *`)
   - **Метод**: GET
3. Сохраните задачу

### Вариант 2: UptimeRobot (Альтернатива)

1. Зарегистрируйтесь на [UptimeRobot](https://uptimerobot.com) (бесплатно, до 50 мониторов)
2. Создайте новый монитор:
   - **Тип**: HTTP(s)
   - **URL**: `https://ваш-сервер.onrender.com/health`
   - **Интервал**: 5 минут
3. Это будет держать сервер "живым"

### Вариант 3: GitHub Actions (Если репозиторий на GitHub)

Создайте файл `.github/workflows/wake-server.yml`:

```yaml
name: Wake Notification Server

on:
  schedule:
    - cron: '*/10 * * * *'  # Каждые 10 минут
  workflow_dispatch:  # Ручной запуск

jobs:
  wake:
    runs-on: ubuntu-latest
    steps:
      - name: Wake server
        run: |
          curl -X GET https://ваш-сервер.onrender.com/wake
```

## Проверка работы

1. Откройте логи сервера в Render.com Dashboard
2. Вы должны видеть сообщения `🔔 Сервер пробужден` каждые 10 минут
3. Проверьте endpoint `/health`:
   ```bash
   curl https://ваш-сервер.onrender.com/health
   ```
4. Должен вернуться JSON с информацией о сервере и расписаниях

## Важно

- **Бесплатный тариф Render.com**: Сервер засыпает после 15 минут бездействия
- **Платный тариф ($7/месяц)**: Сервер работает 24/7 без засыпания
- **Альтернатива**: Railway.app имеет более щедрый бесплатный тариф

## Текущий статус

Сервер уже настроен с:
- ✅ Endpoint `/wake` для пробуждения
- ✅ Endpoint `/health` с информацией о расписаниях
- ✅ Улучшенное логирование
- ✅ Проверка каждую минуту
- ✅ Логирование активных расписаний каждые 5 минут

**Осталось только**: Настроить внешний cron для пробуждения каждые 10 минут.


