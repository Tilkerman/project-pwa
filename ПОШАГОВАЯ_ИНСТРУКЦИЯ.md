# 📋 Пошаговая инструкция: Диагностика уведомлений

## ✅ Что у нас есть

- Chat ID сохранен: `198425911` ✅
- Telegram подключен ✅
- Проблема: Расписание не сохраняется на сервере

---

## 🔍 ШАГ 1: Откройте консоль браузера

1. **Откройте приложение** в Telegram (или в браузере)
2. **Нажмите F12** (или правой кнопкой мыши → "Проверить элемент" → вкладка "Console")
3. **Консоль должна открыться внизу или справа**

---

## 🔍 ШАГ 2: Очистите консоль

1. **Найдите кнопку "Clear console"** (очистить консоль) или нажмите **Ctrl+L**
2. **Консоль должна стать пустой**

---

## 🔍 ШАГ 3: Обновите привычку "На спорте"

1. **В приложении** найдите привычку "На спорте"
2. **Откройте её** (нажмите на неё)
3. **Измените время уведомления** (например, на **15:25**)
4. **Нажмите "Сохранить"** (или кнопку сохранения)

---

## 🔍 ШАГ 4: Смотрите в консоль браузера

**Сразу после сохранения** в консоли должны появиться логи.

### ✅ Если всё хорошо, вы увидите:

```
🔄 Обновление привычки: На спорте { notificationEnabled: true, notificationTime: "15:25" }
📅 Планирование уведомлений для обновленной привычки: На спорте
📋 Разрешение на уведомления: ✅ получено
🚀 Вызов scheduleNotifications для: На спорте
📤 Отправка расписания на сервер для привычки "На спорте"
📋 Детали: { chatId: "198***", chatIdLength: 9, ... }
🔍 scheduleNotificationOnServer вызван: { habitName: "На спорте", chatId: "198***", ... }
📤 Отправка расписания на сервер: https://habit-tracker-notifications.onrender.com/api/schedule
📋 Данные: { localTime: "15:25", utcTime: "11:25", ... }
✅ Расписание успешно отправлено на сервер уведомлений
```

### ❌ Если есть проблема, вы увидите:

```
❌ Chat ID не найден, расписание не отправлено на сервер
```

ИЛИ

```
❌ Не удалось отправить расписание на сервер: Network error
```

ИЛИ

```
❌ Не удалось отправить расписание на сервер: Failed to fetch
```

---

## 🔍 ШАГ 5: Скопируйте логи из консоли

1. **Выделите все логи** в консоли (Ctrl+A)
2. **Скопируйте** (Ctrl+C)
3. **Сохраните** в текстовый файл или отправьте мне

**Важно:** Скопируйте ВСЕ логи, особенно те, что начинаются с эмодзи (🔄, 📤, ✅, ❌)

---

## 🔍 ШАГ 6: Проверьте Chat ID

1. **В консоли браузера** введите (скопируйте и вставьте):

```javascript
const config = JSON.parse(localStorage.getItem('telegram_notification_config') || '{}');
console.log('Chat ID:', config.chatId);
console.log('Полная конфигурация:', config);
```

2. **Нажмите Enter**
3. **Должно показать:** `Chat ID: 198425911`
4. **Скопируйте результат** и отправьте мне

---

## 🔍 ШАГ 7: Проверьте расписание на сервере

1. **Откройте в браузере** (в новой вкладке):
   ```
   https://habit-tracker-notifications.onrender.com/api/schedules
   ```

2. **Должно показать JSON** с расписаниями:

```json
{
  "success": true,
  "total": 1,
  "active": 1,
  "schedules": [
    {
      "id": "...",
      "name": "На спорте",
      "time": "11:25",
      "enabled": true,
      "chatId": "198425911"
    }
  ]
}
```

3. **Если показывает `"total": 0`** - расписание не сохранилось
4. **Скопируйте результат** и отправьте мне

---

## 📋 Что отправить мне

После выполнения всех шагов отправьте мне:

1. **Логи из консоли браузера** (ШАГ 5)
2. **Результат проверки Chat ID** (ШАГ 6)
3. **Результат проверки расписания на сервере** (ШАГ 7)

---

## ✅ Ожидаемый результат

**Если всё работает правильно:**

- ✅ В консоли: логи обновления и отправки на сервер
- ✅ Chat ID: `198425911`
- ✅ На сервере: расписание "На спорте" с временем `11:25` UTC

**Если есть проблема:**

- ❌ В консоли: ошибки (Chat ID не найден, Network error, и т.д.)
- ❌ На сервере: `"total": 0` (расписаний нет)

---

## 🎯 Итог

**Выполните шаги 1-7 и отправьте мне результаты!**

Это поможет понять, на каком этапе возникает проблема.

