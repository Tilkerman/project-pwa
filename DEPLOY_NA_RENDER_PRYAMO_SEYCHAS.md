# 🚀 Деплой сервера на Render.com - ПРЯМО СЕЙЧАС

## ⚡ Быстрый деплой (5 минут)

### Шаг 1: Откройте Render.com Dashboard

1. Перейдите на: **https://dashboard.render.com**
2. Войдите (можно через GitHub - используйте тот же аккаунт, что и для репозитория)

### Шаг 2: Создайте новый Web Service

1. Нажмите кнопку **"New +"** (вверху справа)
2. Выберите **"Web Service"**

### Шаг 3: Подключите репозиторий

1. В разделе **"Connect a repository"**:
   - Если репозиторий уже подключен: выберите `Tilkerman/project-pwa`
   - Если нет: нажмите **"Connect account"** → выберите GitHub → авторизуйтесь → выберите `Tilkerman/project-pwa`

### Шаг 4: Настройте сервис

**ВАЖНО: Заполните ТОЧНО так, как указано ниже!**

| Поле | Значение |
|------|----------|
| **Name** | `habit-tracker-notifications` |
| **Region** | `Frankfurt` (или ближайший к вам) |
| **Branch** | `main` |
| **Root Directory** | `server` ⚠️ **КРИТИЧНО!** |
| **Runtime** | `Node` |
| **Build Command** | `npm install` |
| **Start Command** | `npm start` |

### Шаг 5: Добавьте переменную окружения

1. Прокрутите вниз до раздела **"Environment Variables"**
2. Нажмите **"Add Environment Variable"**
3. Заполните:
   - **Key**: `TELEGRAM_BOT_TOKEN`
   - **Value**: `7985172630:AAFTN9xGM2C-s0ni07VHOcCCZiyL3XbDh4o`

### Шаг 6: Задеплойте!

1. Прокрутите вниз
2. Нажмите кнопку **"Create Web Service"**
3. Подождите 2-3 минуты

### Шаг 7: Получите URL сервера

После успешного деплоя:
1. В Dashboard вы увидите статус **"Live"** (зеленый)
2. Скопируйте URL сервера (будет показан вверху, например: `https://habit-tracker-notifications.onrender.com`)

---

## ✅ Проверка деплоя

### 1. Проверьте статус в Dashboard

- Должен быть статус: **"Live"** (зеленый)
- Не должно быть ошибок в логах

### 2. Проверьте в браузере

Откройте в браузере:
- `https://ваш-сервер.onrender.com/health`
- Должен вернуться JSON: `{"status":"ok","time":"...","schedulesCount":0}`

### 3. Проверьте endpoint пробуждения

Откройте:
- `https://ваш-сервер.onrender.com/wake`
- Должен вернуться: `{"status":"awake","time":"...","message":"Server is awake and ready"}`

---

## 🔧 После успешного деплоя

### Обновите URL в коде (если отличается от дефолтного)

Если ваш URL отличается от `https://habit-tracker-notifications.onrender.com`:

1. Откройте `src/utils/notificationServer.ts`
2. Найдите строку 12:
   ```typescript
   const defaultRenderUrl = 'https://habit-tracker-notifications.onrender.com'
   ```
3. Замените на ваш реальный URL
4. Закоммитьте и запушьте:
   ```bash
   git add src/utils/notificationServer.ts
   git commit -m "Update notification server URL"
   git push
   ```

### Или используйте переменную окружения

1. Создайте файл `.env` в корне проекта:
   ```
   VITE_NOTIFICATION_SERVER_URL=https://ваш-сервер.onrender.com
   ```
2. Пересоберите приложение:
   ```bash
   npm run build
   ```

---

## 🎯 Проверка работы уведомлений

1. Откройте приложение в Telegram Mini App
2. Создайте привычку с уведомлениями
3. Установите время уведомления (например, через 2 минуты)
4. Откройте консоль браузера (F12) → вкладка "Console"
5. Должны увидеть:
   - `📤 Отправка расписания на сервер: ...`
   - `✅ Расписание отправлено на сервер уведомлений`
6. Закройте приложение полностью
7. Подождите указанное время
8. Уведомление должно прийти от бота @habitnotibot

---

## 🐛 Решение проблем

### Проблема: "Build failed"

**Решение:**
- Проверьте, что **Root Directory** = `server` (не пусто!)
- Проверьте логи в Render Dashboard → вкладка "Logs"

### Проблема: "Service crashed"

**Решение:**
- Проверьте логи в Render Dashboard
- Убедитесь, что переменная `TELEGRAM_BOT_TOKEN` добавлена правильно
- Проверьте, что `Start Command` = `npm start`

### Проблема: Сервер засыпает

**Решение:**
- GitHub Actions уже настроен и будет пробуждать сервер каждые 10 минут
- Проверьте: https://github.com/Tilkerman/project-pwa/actions
- Должен быть workflow "Wake Notification Server"

### Проблема: Уведомления не приходят

**Решение:**
1. Проверьте логи сервера в Render Dashboard
2. Проверьте, что расписание сохранено: `https://ваш-сервер.onrender.com/api/schedules`
3. Убедитесь, что `chatId` правильный (откройте приложение в Telegram Mini App)

---

## 📝 Чек-лист деплоя

- [ ] Зарегистрирован на Render.com
- [ ] Создан Web Service
- [ ] Подключен репозиторий `Tilkerman/project-pwa`
- [ ] Root Directory = `server`
- [ ] Build Command = `npm install`
- [ ] Start Command = `npm start`
- [ ] Добавлена переменная `TELEGRAM_BOT_TOKEN`
- [ ] Статус сервера = "Live"
- [ ] `/health` endpoint возвращает JSON
- [ ] URL сервера скопирован
- [ ] URL обновлен в коде (если отличается)
- [ ] GitHub Actions workflow работает (проверка пробуждения)

---

## 🎉 Готово!

После выполнения всех шагов:
- ✅ Сервер задеплоен и работает
- ✅ GitHub Actions пробуждает сервер каждые 10 минут
- ✅ Уведомления будут работать даже когда приложение закрыто

**Следующий шаг:** Протестируйте создание привычки с уведомлениями!


