# 🔍 Инструкция по диагностике проблемы с уведомлениями

## ❌ Проблема

**В логах сервера:** `Всего расписаний в памяти: 0`  
**Нет записей:** `📥 Получен запрос на сохранение расписания`  
**Вывод:** Запрос не доходит до сервера

---

## 📋 Что нужно проверить

### 1. Откройте консоль браузера (F12)

**После обновления привычки "На спорте" должны увидеть:**

```
🔄 Обновление привычки: На спорте { notificationEnabled: true, notificationTime: "15:10" }
📅 Планирование уведомлений для обновленной привычки: На спорте
📋 Разрешение на уведомления: ✅ получено
🚀 Вызов scheduleNotifications для: На спорте
📤 Отправка расписания на сервер для привычки "На спорте"
📋 Детали: { chatId: "123***", ... }
🔍 scheduleNotificationOnServer вызван: { habitName: "На спорте", chatId: "123***", ... }
📤 Отправка расписания на сервер: https://habit-tracker-notifications.onrender.com/api/schedule
📋 Данные: { localTime: "15:10", utcTime: "11:10", ... }
✅ Расписание успешно отправлено на сервер уведомлений
✅ scheduleNotifications завершен для: На спорте
```

---

### 2. Если видите ошибки

**Ошибка 1: Chat ID не найден**
```
❌ Chat ID не найден, расписание не отправлено на сервер
```

**Решение:**
1. Откройте приложение **в Telegram Mini App** (не в браузере!)
2. Или настройте Chat ID вручную в настройках уведомлений
3. Получите Chat ID, написав боту @habitnotibot команду `/start`

---

**Ошибка 2: Chat ID пустой**
```
❌ КРИТИЧЕСКАЯ ОШИБКА: chatId пустой после преобразования!
```

**Решение:**
- Проверьте, что Chat ID сохранен в `localStorage`:
  ```javascript
  const config = JSON.parse(localStorage.getItem('telegram_notification_config') || '{}');
  console.log('Chat ID:', config.chatId);
  ```

---

**Ошибка 3: Ошибка при отправке на сервер**
```
❌ Не удалось отправить расписание на сервер: ...
```

**Проверьте:**
- URL сервера правильный
- Сервер не спит (проверьте `/health`)
- Нет ошибок CORS или сети

---

**Ошибка 4: Разрешение не получено**
```
📋 Разрешение на уведомления: ❌ отклонено
```

**Решение:**
- Для Telegram Mini App это не должно быть проблемой
- Если в браузере - разрешите уведомления в настройках

---

### 3. Проверьте Chat ID

**В консоли браузера выполните:**

```javascript
// Проверка Telegram Mini App
if (window.Telegram?.WebApp?.initDataUnsafe?.user?.id) {
  console.log('✅ Chat ID из Mini App:', window.Telegram.WebApp.initDataUnsafe.user.id);
} else {
  console.log('❌ Chat ID не найден в Mini App');
}

// Проверка сохраненного Chat ID
const config = JSON.parse(localStorage.getItem('telegram_notification_config') || '{}');
console.log('Chat ID из настроек:', config.chatId || 'не найден');
console.log('Полная конфигурация:', config);
```

---

### 4. Проверьте, вызывается ли scheduleNotifications

**В консоли должны быть логи:**
- `🔄 Обновление привычки: На спорте`
- `📅 Планирование уведомлений для обновленной привычки: На спорте`
- `🚀 Вызов scheduleNotifications для: На спорте`

**Если этих логов нет:**
- `updateHabit` не вызывается
- Или `notificationEnabled` = false

---

### 5. Проверьте отправку на сервер

**Должны увидеть:**
- `📤 Отправка расписания на сервер для привычки "На спорте"`
- `🔍 scheduleNotificationOnServer вызван: ...`
- `📤 Отправка расписания на сервер: https://habit-tracker-notifications.onrender.com/api/schedule`

**Если этих логов нет:**
- Chat ID не найден
- Или `scheduleNotifications` не вызывается

---

## 🎯 Что сделать сейчас

1. **Откройте консоль браузера (F12)**
2. **Обновите привычку "На спорте"** (измените время или просто сохраните)
3. **Скопируйте ВСЕ логи из консоли** (особенно те, что начинаются с эмодзи)
4. **Проверьте Chat ID** (выполните код из пункта 3)
5. **Сообщите результаты**

---

## ✅ Ожидаемый результат

После обновления привычки в консоли должны быть:
- Логи обновления привычки
- Логи планирования уведомлений
- Логи отправки на сервер
- Подтверждение успешной отправки

**В логах сервера должны появиться:**
- `📥 Получен запрос на сохранение расписания: ...`
- `✅ Расписание сохранено: "На спорте" на 11:10 для chatId ...`
- `📊 Всего расписаний в памяти: 1`

---

## 🔧 Быстрое решение

**Если Chat ID не найден:**

1. Откройте приложение **в Telegram Mini App** (не в браузере!)
2. Или настройте Chat ID вручную:
   - Откройте "Настройки" → "Настройки уведомлений"
   - Введите Chat ID (получите у бота @habitnotibot командой `/start`)
   - Нажмите "Сохранить"
3. Обновите привычку заново

---

**Все изменения запушены в git. После обновления приложения проверьте консоль браузера!**


