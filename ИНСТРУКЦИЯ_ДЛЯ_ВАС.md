# 🎯 Что делаю Я, а что делаете ВЫ

## ✅ Что уже сделал Я (AI):
- ✅ Написал весь код для Telegram уведомлений
- ✅ Создал файлы конфигурации
- ✅ Подготовил места для токена бота
- ✅ Настроил интерфейс для пользователей

## 📝 Что нужно сделать ВАМ:

### ШАГ 1: Создать бота в Telegram (5 минут)

**Это делается в Telegram, не в коде!**

1. Откройте Telegram (на телефоне или компьютере)

2. В поиске Telegram найдите: **@BotFather**
   - Это официальный бот от Telegram
   - Он уже существует, просто найдите его

3. Нажмите на бота и нажмите кнопку **"Start"**

4. В чате с BotFather отправьте команду:
   ```
   /newbot
   ```

5. BotFather спросит: **"Alright, a new bot. How are we going to call it? Please choose a name for your bot."**
   - Напишите любое имя, например: `Трекер Привычек`
   - Нажмите Enter

6. BotFather спросит: **"Good. Now let's choose a username for your bot. It must end in `bot`. Like this, for example: TetrisBot or tetris_bot."**
   - Напишите username (должен заканчиваться на `bot`)
   - Например: `habit_tracker_bot` или `my_habits_bot`
   - Нажмите Enter

7. **BotFather отправит вам сообщение с токеном!**
   - Выглядит примерно так:
   ```
   Done! Congratulations on your new bot. You will find it at t.me/habit_tracker_bot. Use this token to access the HTTP API:
   
   123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   ```
   - **СКОПИРУЙТЕ ЭТОТ ТОКЕН!** (это длинная строка с цифрами и буквами)

---

### ШАГ 2: Вставить токен в код (2 минуты)

После того как вы получили токен от BotFather, нужно вставить его в 2 файла:

#### 📄 Файл 1: `src/config/telegram.ts`

1. Откройте файл `src/config/telegram.ts` в редакторе кода

2. Найдите строку 6:
   ```typescript
   export const TELEGRAM_BOT_TOKEN = 'YOUR_BOT_TOKEN_HERE'
   ```

3. Замените `YOUR_BOT_TOKEN_HERE` на ваш токен:
   ```typescript
   export const TELEGRAM_BOT_TOKEN = '123456789:ABCdefGHIjklMNOpqrsTUVwxyz'
   ```
   ⚠️ **Вставьте ВАШ реальный токен!** (не копируйте пример)

4. Сохраните файл (Ctrl+S или Cmd+S)

#### 📄 Файл 2: `public/sw-custom.js`

1. Откройте файл `public/sw-custom.js` в редакторе кода

2. Найдите строку 64:
   ```javascript
   const TELEGRAM_BOT_TOKEN = 'YOUR_BOT_TOKEN_HERE'
   ```

3. Замените `YOUR_BOT_TOKEN_HERE` на тот же токен:
   ```javascript
   const TELEGRAM_BOT_TOKEN = '123456789:ABCdefGHIjklMNOpqrsTUVwxyz'
   ```
   ⚠️ **Вставьте тот же токен, что и в первом файле!**

4. Сохраните файл (Ctrl+S или Cmd+S)

---

### ШАГ 3: Пересобрать приложение (1 минута)

Откройте терминал в папке проекта и выполните:

```bash
npm run build
```

Или если запущено в режиме разработки, просто перезапустите:

```bash
npm run dev
```

---

## ✅ Готово!

После этого:
- Пользователям нужно будет только получить свой Chat ID через @userinfobot
- Ввести Chat ID в настройках приложения
- Уведомления будут работать через ваш бот!

---

## ❓ Частые вопросы:

**Q: Я потерял токен, что делать?**  
A: Откройте @BotFather → отправьте `/mybots` → выберите вашего бота → нажмите "API Token"

**Q: Где именно в файле искать строку?**  
A: В `src/config/telegram.ts` - строка 6, в `public/sw-custom.js` - строка 64

**Q: Что если я ошибся с токеном?**  
A: Просто замените токен на правильный и пересоберите приложение

**Q: Нужно ли создавать бота для каждого пользователя?**  
A: НЕТ! Один бот для всех. Каждый пользователь просто получает свой Chat ID.

---

## 🆘 Если что-то не работает:

1. Проверьте, что токен скопирован полностью (без пробелов в начале/конце)
2. Убедитесь, что токен вставлен в ОБА файла (оба должны быть одинаковые)
3. Проверьте, что токен в кавычках: `'ваш_токен_здесь'`
4. Пересоберите приложение после изменений
5. Проверьте токен: откройте в браузере `https://api.telegram.org/botВАШ_ТОКЕН/getMe` (должен вернуть информацию о боте)













