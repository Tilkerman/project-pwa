# 📦 Инструкция: Как выложить приложение на GitHub Pages

## ✅ Что уже сделано:

1. ✅ Создан файл `.github/workflows/deploy.yml` - автоматическая выкладка
2. ✅ Настроена сборка проекта
3. ✅ Приложение готово к выкладке

---

## 🚀 Как выложить на GitHub (пошагово):

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите зеленую кнопку **"New"** (или плюсик → "New repository")
4. Введите название репозитория (например: `habit-tracker` или `priv`)
5. Выберите **Public** (публичный)
6. **НЕ** ставьте галочки на "Add README", "Add .gitignore" и т.д.
7. Нажмите **"Create repository"**

### Шаг 2: Загрузите код на GitHub

#### Вариант А: Через GitHub Desktop (САМЫЙ ПРОСТОЙ)

1. Скачайте GitHub Desktop: https://desktop.github.com/
2. Установите и откройте
3. Войдите в свой GitHub аккаунт
4. Нажмите **"File" → "Add Local Repository"**
5. Выберите папку `C:\Users\z108\PWA\priv`
6. Нажмите **"Publish repository"**
7. Выберите репозиторий, который создали в Шаге 1
8. Нажмите **"Publish"**

#### Вариант Б: Через командную строку (если знаете Git)

```bash
cd C:\Users\z108\PWA\priv
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_НИКНЕЙМ/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
git push -u origin main
```

### Шаг 3: Включите GitHub Pages

1. На странице вашего репозитория на GitHub
2. Нажмите **"Settings"** (вверху справа)
3. В левом меню найдите **"Pages"**
4. В разделе **"Source"** выберите:
   - **"Deploy from a branch"**
   - Ветка: **"main"** или **"master"**
   - Папка: **"/ (root)"**
5. Нажмите **"Save"**

### Шаг 4: Дождитесь автоматической выкладки

1. GitHub автоматически запустит сборку (может занять 2-5 минут)
2. На странице репозитория нажмите вкладку **"Actions"**
3. Дождитесь завершения (зеленая галочка ✅)
4. Вернитесь в **"Settings" → "Pages"**
5. Там будет адрес вашего сайта: `https://ВАШ_НИКНЕЙМ.github.io/НАЗВАНИЕ_РЕПОЗИТОРИЯ/`

---

## 🎉 Готово!

Ваше приложение будет доступно по адресу:
`https://ВАШ_НИКНЕЙМ.github.io/НАЗВАНИЕ_РЕПОЗИТОРИЯ/`

---

## 📝 Что делать дальше:

### После изменений в коде:

1. Соберите проект: `npm run build`
2. Загрузите изменения на GitHub:
   - Через GitHub Desktop: нажмите "Commit" → "Push origin"
   - Или через командную строку: `git add .` → `git commit -m "Update"` → `git push`
3. GitHub автоматически пересоберет и выложит новую версию

---

## ⚠️ Важно:

- Если название репозитория НЕ `project-pwa`, нужно изменить `vite.config.ts`:
  - Найдите строку: `base: process.env.CI ? '/project-pwa/' : '/',`
  - Замените `'/project-pwa/'` на `'/ВАШЕ_НАЗВАНИЕ_РЕПОЗИТОРИЯ/'`

---

## 🆘 Если что-то не работает:

1. Проверьте вкладку **"Actions"** на GitHub - там видны ошибки
2. Убедитесь, что файл `.github/workflows/deploy.yml` загружен на GitHub
3. Проверьте, что в настройках Pages выбрана ветка `main` или `master`


