# 🔍 Диагностика: Уведомления не приходят

## ❌ Проблема: На сервере 0 расписаний

**Проверено:** `https://habit-tracker-notifications.onrender.com/api/schedules`  
**Результат:** Расписаний нет

---

## 🔍 Возможные причины

### 1. Chat ID не найден или не передается

**Проверьте:**
- Откройте консоль браузера (F12)
- Создайте/обновите привычку с уведомлениями
- Должны увидеть:
  - `📤 Отправка расписания на сервер для привычки "На спорте"`
  - `📋 Детали: { chatId: "123***", ... }`
  - `✅ Расписание успешно отправлено на сервер`

**Если видите:**
- `⚠️ Chat ID не найден, расписание не отправлено на сервер` → **Проблема с Chat ID**

**Решение:**
1. Откройте приложение в Telegram Mini App (не в браузере!)
2. Или настройте Chat ID вручную в настройках уведомлений
3. Получите Chat ID, написав боту @habitnotibot команду `/start`

---

### 2. Ошибка при отправке на сервер

**Проверьте консоль браузера:**
- `❌ Не удалось отправить расписание на сервер: ...`
- `❌ Ошибка при отправке расписания на сервер: ...`

**Возможные ошибки:**
- `Network error` → Сервер недоступен или спит
- `Timeout` → Сервер не отвечает
- `400 Bad Request` → Неверные данные (chatId, время)
- `500 Internal Server Error` → Ошибка на сервере

---

### 3. Сервер не получает запрос

**Проверьте логи сервера на Render.com:**
1. Откройте https://dashboard.render.com
2. Выберите сервис "habit-tracker-notifications"
3. Перейдите на вкладку "Logs"
4. Должны увидеть:
   - `📥 Получен запрос на сохранение расписания: ...`
   - `✅ Расписание сохранено: "На спорте" на 10:55 для chatId ...`

**Если не видите этих логов:**
- Запрос не доходит до сервера
- Проверьте URL сервера в консоли браузера

---

### 4. Проблема с конвертацией времени

**Проверьте консоль браузера:**
- `📋 Данные: { localTime: "14:55", utcTime: "10:55" }`

**Если видите ошибку:**
- `Invalid time format` → Неверный формат времени

---

## 🧪 Пошаговая диагностика

### Шаг 1: Проверьте Chat ID

**В консоли браузера выполните:**
```javascript
// Проверка Telegram Mini App
if (window.Telegram?.WebApp?.initDataUnsafe?.user?.id) {
  console.log('Chat ID из Mini App:', window.Telegram.WebApp.initDataUnsafe.user.id);
} else {
  console.log('❌ Chat ID не найден в Mini App');
}

// Проверка сохраненного Chat ID
const config = JSON.parse(localStorage.getItem('telegram_config') || '{}');
console.log('Chat ID из настроек:', config.chatId || 'не найден');
```

---

### Шаг 2: Проверьте отправку на сервер

**Создайте/обновите привычку и смотрите консоль:**
- Должны увидеть `📤 Отправка расписания на сервер`
- Должны увидеть `✅ Расписание успешно отправлено`

**Если видите ошибку:**
- Скопируйте полный текст ошибки
- Проверьте URL сервера

---

### Шаг 3: Проверьте сохранение на сервере

**Откройте в браузере:**
```
https://habit-tracker-notifications.onrender.com/api/schedules
```

**Должно быть:**
```json
{
  "success": true,
  "total": 1,
  "active": 1,
  "schedules": [
    {
      "id": "...",
      "name": "На спорте",
      "time": "10:55",
      "enabled": true,
      "chatId": "..."
    }
  ]
}
```

**Если пусто:**
- Расписание не сохранилось
- Проверьте логи сервера на Render.com

---

### Шаг 4: Проверьте логи сервера

**На Render.com Dashboard:**
1. Откройте логи сервера
2. Найдите записи о сохранении расписания
3. Проверьте ошибки

**Должны увидеть:**
- `📥 Получен запрос на сохранение расписания: ...`
- `✅ Расписание сохранено: ...`

**Если видите ошибку:**
- `❌ Отсутствуют обязательные поля` → chatId не передается
- `❌ chatId пустой` → chatId пустой или неверный
- `❌ Неверный формат времени` → Проблема с временем

---

### Шаг 5: Проверьте работу бота

**Напишите боту @habitnotibot:**
- `/start` → Должен ответить с вашим Chat ID

**Проверьте тестовую отправку:**
```javascript
// В консоли браузера (замените YOUR_CHAT_ID)
fetch('https://habit-tracker-notifications.onrender.com/api/test-notification', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    chatId: 'YOUR_CHAT_ID',
    message: '🧪 Тестовое уведомление!'
  })
})
.then(r => r.json())
.then(console.log)
```

**Должно прийти уведомление в Telegram!**

---

## ✅ Чек-лист

- [ ] Chat ID найден (проверено в консоли)
- [ ] Расписание отправляется на сервер (нет ошибок в консоли)
- [ ] Расписание сохранено на сервере (`/api/schedules` показывает расписание)
- [ ] Логи сервера показывают сохранение
- [ ] Бот отвечает на `/start`
- [ ] Тестовое уведомление приходит

---

## 🎯 Следующие шаги

1. **Откройте консоль браузера (F12)**
2. **Создайте/обновите привычку "На спорте"**
3. **Скопируйте все логи из консоли**
4. **Проверьте `/api/schedules`**
5. **Проверьте логи сервера на Render.com**
6. **Сообщите результаты**

---

## 🔧 Быстрое решение

**Если Chat ID не найден:**

1. Откройте приложение **в Telegram Mini App** (не в браузере!)
2. Или настройте Chat ID вручную:
   - Откройте "Настройки" → "Настройки уведомлений"
   - Введите Chat ID (получите у бота @habitnotibot командой `/start`)
   - Нажмите "Сохранить"
3. Создайте привычку заново

**Если расписание не сохраняется:**

1. Проверьте консоль браузера на ошибки
2. Проверьте логи сервера на Render.com
3. Проверьте, что сервер не спит (должен отвечать на `/health`)

